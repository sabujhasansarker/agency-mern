import React, { Fragment } from "react";
import Banner from "../template/Banner";

const Home = () => {
   return (
      <Fragment>
         <Banner />
      </Fragment>
   );
};

export default Home;
